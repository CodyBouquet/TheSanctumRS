[build]
  command = "npm run build"
  publish = ".next"

  ignore = "bash -lc 'CHANGED=$(git diff --name-only \"$CACHED_COMMIT_REF\" \"$COMMIT_REF\"); if echo \"$CHANGED\" | grep -qv \"^ge-bot/\"; then exit 1; else exit 0; fi'"

[[plugins]]
  package = "@netlify/plugin-nextjs"
